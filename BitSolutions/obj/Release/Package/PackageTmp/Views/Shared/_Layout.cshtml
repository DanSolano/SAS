<!DOCTYPE html>
<html>
<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Manejo de tickets Bitsolutions</title>
    <link rel="icon" type="image/png" href="img/favicon.png">
    <link rel="apple-touch-icon-precomposed" href="img/apple-touch-favicon.png">
    <link href="http://fonts.googleapis.com/css?family=Roboto:400,100,100italic,300,300italic,400italic,500,500italic,700,700italic,900,900italic" rel="stylesheet" type="text/css">
    @Styles.Render("~/Content/css")
    @*@Styles.Render("~/Content/BaseCss");*@

    @RenderSection("Styles", required: false)
    @Scripts.Render("~/bundles/modernizr")

</head>
<body class="framed main-scrollable">

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")

    <div class="wrapper">
        @RenderBody()
    </div>

    @*@Scripts.Render("~/bundles/BaseJS");*@

    @Html.Partial("~/Views/Themes/Theme.cshtml");

    @RenderSection("scripts", required: false)
</body>
</html>

<script>
    @*$(document).ready(function () {

        $('#slFilter').on('change', function () {
            //let optionText = $("#slFilter option:selected").text();

            var optionValue = $(this).val();

            $.ajax({
                url: '@Url.Action("SearchTicketByParameter", "SAS")',
                data: { searchParameter: optionValue }
            }).done(function () {
                
            });
        });
    });*@
</script>













